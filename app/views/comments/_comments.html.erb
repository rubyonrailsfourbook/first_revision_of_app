<div class="comment">
	<div class="created_at"><%= post.created_at.strftime("%B %d, %Y") %></div>
	<div class="content">
		<%= comment.content %>
	</div>
	<div class="actions">
		<%= link_to "Reply", new_post_url(parent_id: post)
	</div>
</div>